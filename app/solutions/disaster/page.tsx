import DisasterTracks from "@/components/screens/solutiontracks/DisasterTracks";

export const metadata = {
  title: "Disaster Documentation Readiness | ClaimScope Consulting",
  description:
    "ClaimScope advisory services for disaster documentation readiness and FEMA IA/PA education.",
};

export default function DisasterTracksPage() {
  return <DisasterTracks />;
}
